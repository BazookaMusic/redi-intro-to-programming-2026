[pytest]
# Every exercise has a file called test_exercise.py. importlib mode lets
# pytest run tests from many folders together even though the names repeat.
addopts = --import-mode=importlib -p no:cacheprovider
