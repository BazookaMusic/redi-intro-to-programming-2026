# Clio

A local Slidev theme using the BazookaMusic visual palette.

Apply it to a deck with a path relative to that deck:

```yaml
---
theme: ../../themes/clio
---
```

Other decks can select `theme: default` or any other Slidev theme independently.